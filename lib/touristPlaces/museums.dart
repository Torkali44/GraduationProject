
import 'package:flutter/material.dart';

class MuseumsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('المتاحف')),
      body: const Center(child: Text('صفحة المتاحف')),
    );
  }
}