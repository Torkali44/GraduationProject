
import 'package:flutter/material.dart';

class IslamicMonumentsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('الآثار الإسلامية')),
      body: const Center(child: Text('صفحة الآثار الإسلامية')),
    );
  }
}
