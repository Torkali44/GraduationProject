import 'package:flutter/material.dart';


class CopticMonumentsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('الآثار القبطية')),
      body: const Center(child: Text('صفحة الآثار القبطية')),
    );
  }
}
